var repositorySample = require('./repository')
var webhookSample = require('./webhook')
var issueSample = require('./issue')

module.exports = {
    repository: repositorySample,
    webhook: webhookSample,
    issue: issueSample
}